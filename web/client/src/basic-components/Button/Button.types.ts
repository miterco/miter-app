export enum AntButtonType {
  link = 'link',
  text = 'text',
  ghost = 'ghost',
  default = 'default',
  primary = 'primary',
  dashed = 'dashed',
}

export enum ButtonType {
  primary = 'primary',
  default = 'default',
  borderless = 'borderless',
  placeholder = 'placeholder',
}

export enum ButtonSize {
  large = 'large',
  small = 'small',
}

export enum ButtonVariant {
  default = 'default',
  outline = 'outline',
}
